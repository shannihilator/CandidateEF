import React, { Component } from 'react'
import { Button } from 'semantic-ui-react'

export default class UniqueCharacters extends Component {
  render() {
    return (
      <div>
        <Button size='big'>Calculate Unique Characters</Button>
      </div>
    )
  }
}
